/**
 * Semantic color for Parking indicators.
 *
 * This is the single source of truth for the CSS color variable used to
 * highlight Parking values (P1-P8) across the app. All parking codes share
 * one semantic color, mirroring the parking color-coding used in the source
 * XLSX RoadBook; the P1-P8 codes themselves keep their existing meaning.
 */
export const PARKING_COLOR_VAR = "--color-parking";

